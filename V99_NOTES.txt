v99:
- Old brackets (base_version 1 or missing) are forced to render from Base 1.
- This avoids any lingering Base 2 contamination in stored base data from earlier tests.
- New brackets (base_version 2+) still use their own saved base / Base 2.
- A corrected base is persisted once after load.
